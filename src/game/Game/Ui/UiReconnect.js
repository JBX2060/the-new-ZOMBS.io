import UiComponent from "Game/Ui/UiComponent";
import template from "template/ui-reconnect.html";

class UiReconnect extends UiComponent {
  constructor(ui) {
    return super(ui, template);
  }
}

export default UiReconnect;
