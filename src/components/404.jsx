import React from "react";

class Error extends React.Component {
  render() {
    return (
      <div className="error">
        We couldn't find any pages matching that web address.
      </div>
    );
  }
}

export default Error;
